# Copilot Instructions

## Project Guidelines
- For the Saria mod body mask system: ALL mask types (Mask1, Mask2, Mask3) should be applied to ALL body parts (body, eat, direction arms, idle legs, idle arms). Don't limit any mask to a subset of body parts - when the user says "all body parts" they mean ALL.